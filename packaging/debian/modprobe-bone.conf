# uio_pdrv_genirq binds by a compatible string it takes from this parameter,
# having none of its own. The cape overlay's PRU event node declares
# "generic-uio", so without this there is no /dev/uio for PRU events and the
# bus engine never hears from the PRUs.
options uio_pdrv_genirq of_id=generic-uio
