# Commands fed to the emulator at startup, one per line, as if typed at its
# menu. "d" enters device emulation, which is what a running machine needs.
#
# The web interface applies saved configurations, so device setup belongs
# there rather than here.
d
